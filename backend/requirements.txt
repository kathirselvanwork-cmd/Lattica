# Lattica Backend Dependencies
# ============================

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database ORM
sqlalchemy>=2.0.0
aiosqlite>=0.20.0      # Async SQLite driver for SQLAlchemy

# TLS/SSL scanning engine — the core detection layer
sslyze>=6.0.0

# LLM providers — powers the agentic remediation layer (Day 5)
# Provider-agnostic: users pick gemini, openai, claude, or ollama
# Only the selected provider's SDK needs to be installed
google-genai>=1.0.0    # Google Gemini (free tier available — recommended)
anthropic>=0.52.0      # Anthropic Claude
# openai>=1.0.0        # OpenAI (uncomment if needed)
httpx>=0.27.0          # Used by Ollama provider (also an httpx dep of others)

# CORS support for React frontend
# (included in fastapi, but explicitly noting it here)

# Utilities
pydantic>=2.0.0         # Already a FastAPI dep, but pinning for clarity
python-dotenv>=1.0.0    # For loading .env config (API keys, etc.)
